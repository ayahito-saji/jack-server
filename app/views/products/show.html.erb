<div>
  <%= @id %>
  <h1 contenteditable="true">
    <%= @product.name %>
  </h1>
  <div>
    <%= "作成日時#{@product.created_at}" %>
    <%= link_to("編集","/products/#{@product.id}/edit") %>
  </div>
</div>
