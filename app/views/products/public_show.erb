<div class = "main products-show">
  <div class = "container">
    <div class ="products-show-item">
      <h1><%= @product.name %></h1>
      <h2>プロダクト内容</h2>
      <p><%= @product.content %></p>
      <%= "作成日時#{@product.created_at}" %>
    </div>
  </div>
</div>